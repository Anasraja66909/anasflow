[build]
base = "frontend-next/"
command = "npm run build"
publish = ".next"
environment = { NODE_VERSION = "20.10.0" }

[context.production.environment]
NODE_ENV = "production"
NEXT_PUBLIC_API_URL = "https://api.anasflow.app"
